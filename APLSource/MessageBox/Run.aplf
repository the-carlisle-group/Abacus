 Run←{
     ⍺←1
     ~⍺:0
     m bn←2↑(⊆⍵),⊂'OK' 'Cancel'
     ⎕THIS.H←##.Main
     f←H.NewDocument 0
     b←f H.ElementByTag'body'
     b.Onkeydown←'OnKeyDown'
     p←b H.New'p'm
     d←b H.New'div'
     d.class←'buttons'
     be←{d H.New'button'⍵}¨bn
     (⊃be).autofocus←'autofocus'
     be.Onclick←⊂'OnClick'
     f.Style←BuildCSS 0
     f.Size←400 900
     r←H.NewModalForm f
     r.Result←¯1
     r.Caption←'Warning'
     rc←r.Wait
     r.Result
 }
