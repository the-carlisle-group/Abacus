 CSS←{
     Rule←##.NewRule
     ⍝ *********************
     s←Rule'.tabs'
     ⍝ *********************
     r←s Rule'>nav>button'
     r.border←'none'
     ⍝ ******************
     r←s Rule'>section'
     r.flex←1
     ⍝ Active button
     r←s Rule'.active-tab'
     r.color←'var(--selected-text-color)'
     r.background_color←'var(--selected-background-color)'
     ⍝ **************
     A.UnNestCSS s
 }
