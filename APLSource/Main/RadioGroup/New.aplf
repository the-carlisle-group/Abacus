 New←{
     ⍺←0
     (p n)←A.DefaultParentAndName ⍺
     d←A.InitComp(p A.New'fieldset')⎕NS(
         'Legend' ''
         'Labels' ''
         'Options' ''
         'Value' ''
         'Border' 1
         'Disabled' 0
         'OnChange' ''
     )A.Default ⍵
     d.Name←n
     _←d A.New'legend'd.Legend
     i←A.New¨{'input' ''}¨d.Labels
     c←(d.Options⍳⊂d.Value)⊃i,i
     c.checked←'checked'
     i.Value←d.Options
     i.name←⊂n
     i.type←⊂'radio'
     i.id←A.GetId¨0⍴⍨≢i
     l←A.New¨{'label'⍵}¨d.Labels
     l.for←i.id
     d.Content,←A.New¨{'div'⍵}¨↓⍉↑i l
     d.Onchange←A.FQP'OnChange'
     d
 }
