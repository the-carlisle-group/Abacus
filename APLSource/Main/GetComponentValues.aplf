 GetComponentValues←{
     ⍝ ⍺|⍵ ←→ DOM node
     ⍝ ← ←→ Namespace of Values from ⍵
     ⍺←⍵
     e←⍺ GetElementsWith'Name'
     0=≢e:()
     c←e/⍨2.1=e.⎕NC⊂⊂'Value'
     0=≢c:()
     +()⎕VSET(↑c.Name)c.Value
 }
