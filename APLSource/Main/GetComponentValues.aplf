 GetComponentValues←{
     ⍝ ⍺|⍵ ←→ DOM node
     ⍝ ← ←→ Name/Value pairs
     ⍺←⍵
     e←⍺ GetElementsWith'Name'
     0=≢e:0/⊂'' ''
     c←e/⍨2.1=e.⎕NC⊂⊂'Value'
     0=≢c:0/⊂'' ''
     c.Name{⍺ ⍵}¨c.Value
 }
