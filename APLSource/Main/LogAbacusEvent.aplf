 LogAbacusEvent←{
     d←⍵.Document
     ~d.LogAbacusEvents:0
     c←⍵.Component
     en←'On',⍵.Event
     n←en'Type' 'Name' 'Value'
     v←c ⎕VGET{⍵'{none}'}¨n
     v[0]←⊂⍺,'{no callback}'/⍨0=≢⍺
     m←¯2↓∊n{⍺,': ',⍵,', '}¨v
     ⎕←m
     0
 }
