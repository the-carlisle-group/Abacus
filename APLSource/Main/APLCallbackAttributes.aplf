 APLCallbackAttributes←{
     n←'O'⍵.⎕NL ¯2
     0=≢n:0 2⍴⊂''
     j←'sendAPLRequest(event)'
     a←↑⍵{(⎕C ⍵)j}¨n
     2=⍵.⎕NC'id':a
     ⍵.id←GetNextId 0
     a
     ⍝ 'id'⍵.id⍪↑⍵{(⎕C ⍵)j}¨n   ⍝ Will get added later
 }
