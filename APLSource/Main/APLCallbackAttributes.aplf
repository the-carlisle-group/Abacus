 APLCallbackAttributes←{
     q←''''
     n←'O'⍵.⎕NL ¯2
     0=≢n:0 2⍴⊂''
     ↑⍵{
         e←q,⍵,q
         m←⎕C
         c←q,(⍺⍎⍵),q
         (⎕C ⍵)('sendAPLRequest(event,',c,')')
         ⍝ (⎕C ⍵)('sendAPLRequest(event, )')
     }¨n
 }
