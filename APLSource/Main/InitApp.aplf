 InitApp←{
     ⍝ ⍵ ←→ Document
     d←⍵
     d.LastTID←{}&0
     d.SessionID←NewGUID'N'
     d.Script←BuildJavaScript d
     d.NextId←0
     d.Waiting←0
     d.StopPropagation←0
     b←GetBody d
     b.Content←b.Content,⍨AutoComplete.New''
     e←Elements d
     e.Document←d
     _←d SetDefaultId e
     _←SetParent d
     d
 }
