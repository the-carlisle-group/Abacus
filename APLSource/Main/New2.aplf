 New2←{
     ⍝ ⍺ ←→ [Parent, 0 - no parent]
     ⍝ ← ←→ Element
     ⍺←0
     e←(
         'Tag' ''
         'Content' ''
     )Default ⍵
     _←e.⎕DF e.Tag
     e.Tag←,e.Tag
     e.Parent←⍺
     e⊣⍺ Add e
 }
