 ReplaceAll←{
     ⍝ ⍺ ←→ Search pattern, transformation pattern, hit count of hits
     ⍝ ⍵ ←→ Column space, Indices
     c h←⍵
     i←⍸,h≠0
     0=≢i:0
     sp tp←⍺
     m←c.Type≡'Char'
     v←A.ntbs⍣m⊢c.Value
     v[i]←(sp ⎕R tp)v[i]
     c.Value←↑⍣m⊢v
     0
 }
