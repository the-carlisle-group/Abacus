 OnFind←{
     _←OnNext ⍵           ⍝ Or OnFirst
     d←⍵.Target A.GetNearest'section'
     fs←SpellFoundSummary d
     fd←SpellFoundDetail d
     fm←(⊂fs),fd
     d.Document A.AlertBox.Show'Found'fm
 }
