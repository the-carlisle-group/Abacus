 OnMouseOver←{
     i←⍵.CurrentTarget
     m←i.Parent
     c←m.Content
     f←⊃(c.Selected,1)/c,i
     _←f i ##.MoveClass'sel-item'
     (f i).Selected←0 1
     i.Type≡'Menu':ShowSubMenu i
     0
 }
