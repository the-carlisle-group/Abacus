 OnClickMenuBar←{
     mi←⍵.CurrentTarget
     mb←mi.Parent
     i←mb.Content⍳mi
     f←⍎mb.BuildFunction
     m←f 0
     s←⊃⌽i⊃m.Content.Content.Content
     s←4⊃⊃i⊃m.Content.Content.Content
     s.MenuBar←mb
     s.Submenu←0
     mb.ActiveIndex←i
     ⍝ mb ⍵ Show2 s

     ⍵ Show s
 }
