 ShowMenuBarMenu←{
     mb←⍵
     i←mb.ActiveIndex
     f←⍎mb.BuildFunction
     m←f 0
     s←4⊃⊃i⊃m.Content.Content.Content
     s.MenuBar←mb
     s.Submenu←0
     a←i⊃mb.Content
     s ShowMenu a
 }
