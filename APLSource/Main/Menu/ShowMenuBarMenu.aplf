 ShowMenuBarMenu←{
     mb←⍵
     i←mb.ActiveIndex
     f←⍎mb.BuildFunction
     m←f ⍵.Document
     s←4⊃⊃i⊃m.Content.Content.Content
     s.MenuBar←mb
     a←i⊃mb.Content
     s ShowMenu a
 }
