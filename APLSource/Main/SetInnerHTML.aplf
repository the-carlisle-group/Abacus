 SetInnerHTML←{
     ⍝ ⍺ ←→ Element
     ⍝ ⍵ ←→ DOM or HTML
     d h←DOMandHTML ⍵
     _←⍺ SetProperty'innerHTML'(h~⎕UCS 13)
     ⍺.Content←d
     _←SetParent ⍺
     e←∊Elements¨d
     0=≢e:0
     e.Document←⍺.Document
     0
 }
