 SetInnerHTML←{
     ⍝ ⍺ ←→ Element
     ⍝ ⍵ ←→ DOM or HTML
     d h←⍺.Document DOMandHTML ⍵
     _←⍺ SetProperty'innerHTML'(Sanitize h)
     ⍺.Content←d
     _←SetParent ⍺
     _←⍺.Document SetResizeObserver ⍺
     0
 }
