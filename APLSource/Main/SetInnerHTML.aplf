 SetInnerHTML←{
     ⍝ ⍺ ←→ Element
     ⍝ ⍵ ←→ DOM or HTML
     d h←DOMandHTML ⍵
     _←⍺ SetProperty'innerHTML'(h~⎕UCS 13)
     ⍺.Content←d
     _←SetParent ⍺
     0=≢d:0
     e←Elements¨d
     e.Document←⍺.Document
     0
 }
