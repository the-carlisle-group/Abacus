 Default←{
     ⍝ ⍺ ←→ Default name/value pairs
     ⍝ ⍵ ←→ Given argument
     ⍝ ← ←→ A new space with ⍺ overiddden by ⍵
     ⍝ See also DVP
     d←()⎕VSET ⍺
     9=⎕NC'⍵':d ⎕NS ⍵
     m←⊃¨⍺
     n←' '~⍨¨(≢⍵)↑m
     p←n{
         ((,2)≡⍴⍵)∧'_'≡⊃⊃⍵:⍵
         ((,2)≡⍴⍵)∧(m∊⍨⊂⊃⍵):⍵
         ⍺≡'':⍺
         ⍺ ⍵}¨⍵
     d ⎕NS()⎕VSET p~⊂''
 }
