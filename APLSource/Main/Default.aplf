 Default←{
     ⍝ ⍺ ←→ Default name/value pairs
     ⍝ ⍵ ←→ Given argument
     ⍝ ← ←→ A new space with ⍺ overiddden by ⍵
     ⍝ See also DVP
     d←()⎕VSET ⍺
     9=⎕NC'⍵':d ⎕NS ⍵
     w←⊆⍣(0≠≢⍵)⊣⍵
     m←⊃¨⍺
     n←' '~⍨¨(≢w)↑m
     p←n{
         IsNameValuePair ⍵:⍵
         ⍺≡'':⍺
         ⍺ ⍵}¨w
     d ⎕NS()⎕VSET p~⊂''
 }
