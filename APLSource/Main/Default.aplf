 Default←{
     ⍝ ⍺ ←→ Default name/value/[required] pairs
     ⍝ ⍵ ←→ Given argument
     ⍝ ← ←→ A new space with ⍺ overiddden by ⍵
     d←()⎕VSET 2↑¨⍺
     9=⎕NC'⍵':d ⎕NS ⍺ Verify ⍵
     w←⊆⍣(0≠≢⍵)⊣⍵
     m←⊃¨⍺
     n←' '~⍨¨(≢w)↑m
     p←n{
         IsNameValuePair ⍵:⍵
         ⍺≡'':⍺
         ⍺ ⍵}¨w
     d ⎕NS ⍺ Verify()⎕VSET p~⊂''
 }
