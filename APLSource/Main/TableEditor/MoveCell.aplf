 MoveCell←{
     ⍝ ⍺ ←→ Table
     ⍝ ⍵ ←→ New Location
     m←A.BodyCells ⍺
     c←⍺{3::0 ⋄ m[⍺.CurCell ⍵]}⍵
     c≡0:0
     oc nc←c
     _←nc A.SetClass'curcell'
     _←oc A.RemoveClass'curcell'
     ⍺.CurCell←⍵
     _←A.ScrollIntoView 1⊃c
     (1⊃c)A.FireEvent'focus'
     0
 }
