 MoveRowDown←{
     ri ci←⍺.CurCell
     tr←A.BodyRows ⍺
     ri=¯1+≢tr:0
     r1 r2←(A.BodyRows ⍺)[ri,ri+1]
     c←r1.Content
     _←r1 A.SetInnerHTML r2.Content
     _←r2 A.SetInnerHTML c
     ⍺.CurCell+←1 0
     _←A.ScrollIntoView ci⊃c
     (ci⊃c)A.FireEvent'focus'
     0
 }
