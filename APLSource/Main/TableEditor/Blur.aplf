 Blur←{
     i←⍵
     i.value≡'crap':GoBackToCell i
     t←i A.GetNearest'table'
     t.CellMode←0
     t.Changed←1
     e←i A.GetNearest'td'
     _←e A.SetInnerHTML i.value
     e A.RemoveClass'incell'
 }
