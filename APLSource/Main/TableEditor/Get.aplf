 Get←{
     ⍝ Get data from table element
     ⍝ ⍺ ←→ Table Element
     ⍝ ⍵ ←→ n/a
     s←⎕NS''
     td←A.BodyCells ⍺
     th←,A.HeaderCells ⍺
     v←td.Value
     s.Values←(~∧/v≡¨⊂'')⌿v
     s.Types←th.Type
     s.Names←th.Name
     s.Formats←th.Format
     s
 }
