 DOMandHTML←{
     ⍝ ⍺ ←→ Document
     ⍝ ⍵ ←→ DOM or HTML
     ⍝ ← ←→ DOM and HTML
     326=⎕DR ⍵:⍵(DOM2HTML ⍺ InitDOM ⍵)
     ''≡⍵:⍵ ⍵
     '<'≢⊃⍵:(,⊂⍵)⍵
     (HTML2DOM ⍵)⍵
 }
