 NewDefinitionListAsTable←{
     ⍝ ⍵ ←→ Names Values
     ⍝ ← ←→ Table
     n v←⍵
     ⍺←0
     newRow←{⍺ New'tr'}
     newCell←{New'td'⍵}
     t←⍺ New'table'
     t.class←'dl'
     tb←t New'tbody'
     cd←↓newCell¨⍉↑n v
     cd.class←⊂'dt' 'dd'
     r←tb newRow¨⍳≢n
     r.Content←cd
     t
 }
