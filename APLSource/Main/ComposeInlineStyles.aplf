 ComposeInlineStyles←{
     ⍝ ⍵ ←→ Namespace of properties and values
     ⍝ _flex_direction ←→ flex-direction
     ⍝ ← ←→ String like 'p1:v1;p2:v2..etc'
     n←⎕C⍛(≡¨)⍛/'_'⍵.⎕NL ¯2
     v←⍵ ⎕VGET n
     f←{1↓'-'@('_'∘=)⍵}
     p←f¨n
     m←(⍉↑p v)⍪⍵.InlineStyles
     1↓⊃,/';',¨{⍺,':',⍵}/(≠m[;0])⌿m
 }
