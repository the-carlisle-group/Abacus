 New←{
     ⍝ ⍺ ←→ [Parent, 0 - no parent]
     ⍝ ← ←→ Element
     ⍺←0
     e←(
         'Tag' ''
         'Content' ''
         'Attributes'(0 2⍴⊂'')
         'InlineStyles'(0 2⍴⊂'')
     )Default ⍵
     _←e.⎕DF e.Tag
     e.Tag←,e.Tag
     e.Parent←⍺
     c←e.Content
     e.Content←''
     e⊣Add/⍺ e c
 }
