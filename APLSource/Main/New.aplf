 New←{
     ⍝ ⍺ ←→ [Parent]
     ⍝ ⍵ ←→ Type [Content] [Attributes]
     ⍝ ← ←→ Element
     ⍺←0
     s←IsString ⍵
     t c a←s⊃(3↑⍵,⊂⍬)(⍵''⍬)
     e←NewElement t
     _←a SetAttributes e
     e⊣Add/⍺ e c
 }
