 New←{
     ⍝ ⍺ ←→ [Parent, 0 - no parent]
     ⍝ ← ←→ Element
     ⍺←0
     e←(
         'Tag' ''
         'Content' ''
     )Default ⍵
     _←e.⎕DF e.Tag
     e.Tag←,e.Tag
     e.Parent←⍺
     c←e.Content
     e.Content←''
     e⊣Add/⍺ e c
 }
