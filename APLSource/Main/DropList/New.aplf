 New←{
     ⍺←0
     d←(⍺ A.New'div')⎕NS(
         'Name' ''
         'Label' ''
         'Value' ''
         'Options' ''
         'OnChange' ''
         'Disabled' 0
     )A.Default ⍵
     d.class←'DropList'
     l←d A.New'label'd.Label
     l.for←d.Name
     op←A.New¨{'option'⍵}¨d.Options
     s←d A.New'select'op
     s.id←d.Name
     _←s ⎕VSET d.Disabled/⊂2⍴⊂'disabled'
     d.Onchange←A.FQP'OnChange'
     _←d InitValue d.Value
     d
 }
