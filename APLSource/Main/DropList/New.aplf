 New←{
     ⍺←0
     d←(⍺ A.New'div')⎕NS(
         'Name' ''
         'Label' ''
         'Value' ''
         'Options' ''
         'OnChange' ''
     )A.Default ⍵
     d.class←'DropList'
     l←d A.New'label'd.Label
     l.for←d.Name
     op←A.New¨{'option'⍵}¨d.Options
     cs←(d.Options⍳⊂d.Value)⊃,⍨op
     cs.selected←'selected'
     s←d A.New'select'op
     s.id←d.Name
     d.Onchange←A.FQP'OnChange'
     d
 }
