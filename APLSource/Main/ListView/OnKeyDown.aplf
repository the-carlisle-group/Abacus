 OnKeyDown←{
     t←⍵.CurrentTarget
     k←⍵.Key
     k⊂⍛∊'Control' 'Shift' 'Alt' 'Process':0
     b←⍵.(CtrlKey ShiftKey AltKey)∊⊂⊂'true'
     p←k,⍨∊b/'Ctrl' 'Shift' 'Alt'
     p⊂⍛∊'ArrowUp' 'ArrowLeft':t DebounceSelect t MoveTo ¯1+t.ActiveIndex
     p⊂⍛∊'ArrowDown' 'ArrowRight':t DebounceSelect t MoveTo 1+t.ActiveIndex
     0
 }
