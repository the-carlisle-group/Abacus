 NewRule←{
     ⍝ ⍺ ←→ [Parent]
     ⍝ ⍵ ←→ Selector
     ⍝      e.g. 'h1' 'h1 h2' 'header>h1', etc.
     ⍺←0
     r←⎕NS''
     _←r.⎕DF'CSSRule'
     r.Selector←⍵
     r.Rules←⍬
     ⍺=0:r
     ⍺.Rules,←r
     r
 }
