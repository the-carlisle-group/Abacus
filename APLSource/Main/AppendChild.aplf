 AppendChild←{
     ⍝ ⍺ ←→ Parent
     ⍝ ⍵ ←→ Element
     _←⍺ ExecuteOnElement'appendChild(document.createElement("',⍵.Tag,'"))'
     ⍵.Parent←⍺
     ⍺.Content,←⍵
     ⍵.Document←⍺.Document
     ⍵ SetOuterHTML ⍵
 }
