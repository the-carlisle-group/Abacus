 Run←{
     ⍝ ⍺⍺ ←→ Iteration Function
     ⍝ ⍺ ←→ Progress bar
     ⍝ ⍵ ←→ Suitable argument for ⍺⍺
     _←⍺ Show 0
     r←⍺ ⍺⍺{
         s←((2=≡⍺.Status)/1+⍺.Iteration)⊃⍺.Status
         c←⍺ Update s
         c≡'Cancel':c ⍺.Result
         c≡'Truncate':c ⍺.Result
         ⍺.Result,←⊂⍺ ⍺⍺ ⍵
         ⍺.Exit:c ⍺.Result
         ⍺.Iteration=⍺.Iterations-1:c ⍺.Result
         ⍺ ∇ ⍵
     }⍵
     r⊣A.DeleteElement ⍺
 }
