 OnPause←{
     ⍝ On Pause, or On Resume!
     d←⍵.CurrentTarget A.GetNearest'dialog'
     b←d A.GetElementById'progress-pause'
     _←b A.SetInnerHTML d.Paused⊃'Resume' 'Pause'
     ~d.Paused:d.Paused←1
     1⊣'Resume'⎕TPUT d.PauseToken
 }
