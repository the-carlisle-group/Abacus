 Update←{
     ⍝ ⍺ ←→ ProgressBar instance
     ⍝ ⍵ ←→ Message
     ⍝ ← ←→ 'Cancel'|'Resume'|'Truncate'
     p←⍺
     p.Status←(2=≡p.Status)⊃⍵ p.Status
     p.Iteration+←1
     0≠p.UpdatePeriod|p.Iteration:'Resume'
     _←p UpdateStatus ⍵
     p.Canceled:'Cancel'
     p.Truncated:'Truncate'
     ~p.Paused:'Resume'
     r←⊃⎕TGET p.Document.ProgressToken
     p.Paused←0
     r
 }
