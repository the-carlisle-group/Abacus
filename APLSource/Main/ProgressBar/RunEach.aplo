 RunEach←{
     ⍺.Iteration←⍵
     p←⍺.ProgressBar
     p.Canceled:1 0
     _←p UpdateStatus ⍵⊃⍺.Status
     ~p.Cancel:p ⍵ UpdateProgress ⍺ ⍺⍺ ⍵
     r←⍺.Document A.ConfirmBox.Show ⍺.Message
     p.Cancel←0
     r≢⊂'Yes':p ⍵ UpdateProgress ⍺ ⍺⍺ ⍵
     p.Cancel←1
     p.Canceled←1
     1 0
 }
