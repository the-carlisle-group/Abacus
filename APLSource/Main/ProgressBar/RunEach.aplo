 RunEach←{
     ⍝ w ←→ Right arg to ⍺⍺
     ⍝ ⍵ ←→ Iteration #
     ⍝ p ←→ ProgressBar component
     p w←⍺
     p.Cancel:1 0
     c←p Pause 0
     c≡'Cancel':1 0⊣p.Cancel←1
     c≡'Finish':2 0⊣p.Cancel←1
     _←p Update ⍵⊃p.Iterator
     r←⍵ ⍺⍺ w
     0 r
 }
