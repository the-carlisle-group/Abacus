 New←{
     d←(⍺ A.New'div')⎕NS(
         'Name' ''
         'Label' ''
         'Value' 0 ⍝ YYYYMMDD or 0 for null
         'OnChange' ''
     )A.Default ⍵
     d.class←'DateInput'
     l←d A.New'label'd.Label
     l.for←d.Name
     i←d A.New'input'
     i.type←'date'
     i.id←d.Name
     d.Onchange←A.FQP'OnChange'
     _←d InitValue d.Value
     d
 }
