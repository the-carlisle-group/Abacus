 OnKeyDown←{
     l←⍵.CurrentTarget
     k←⍵.Key
     b←⍵.(CtrlKey ShiftKey AltKey)∊⊂⊂'true'
     p←k,⍨∊b/'Ctrl' 'Shift' 'Alt'
     p≡'Enter':
     p≡'ArrowUp':
     p≡'ArrowDown':
     ⍝ Tab and shift tab too
     0
 }
