 Init←{
     ⍝ ⍺ ←→ Document
     ⍝ ⍵ ←→ Propspace: Title,Subtitle,Content,OnApply
     A←##
     p←⍵
     d←⍺ A.ElementById'inline-dialog'
     s←A.New'section'
     s.Content←p.Content   ⍝ Maybe just add p.Content directly, no need for extra section
     a←A.New'div'
     a.class←'actions'
     b←a A.New¨{'button'⍵}¨'Apply' 'x'
     b.Onclick←p.OnApply(A.FQP'OnClose')
     d A.SetInnerHTML s,a
 }
