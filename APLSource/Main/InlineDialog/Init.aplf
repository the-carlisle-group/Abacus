 Init←{
     ⍝ ⍺ ←→ Document
     ⍝ ⍵ ←→ Propspace: Title,Subtitle,Content,OnApply
     A←##
     p←⍵
     d←⍺ A.ElementById'inline-dialog'
     h←A.New'header'
     h1←h A.New'h1'p.Title
     b←h A.New¨{'button'⍵}¨'Apply' 'x'
     b.Onclick←p.OnApply(A.FQP'OnClose')
     s←A.New'section'
     s.Content←p.Content
     d A.SetInnerHTML h,s
 }
