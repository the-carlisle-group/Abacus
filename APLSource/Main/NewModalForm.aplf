 NewModalForm←{
     ⍝ ⍺ ←→ Sizeable
     ⍝ ⍵ ←→ Document
     ⍺←0
     p←⊂'HTMLRenderer'
     p,←⊂'Sizeable'⍺
     p,←⊂'MinButton' 0
     p,←⊂'MaxButton'⍺
     p,←⊂'Coord' 'RealPixel'
     p,←⊂'Size'⍵.Size
     n←NextFormName 0
     h←⍎n ⎕WC p
     h.InterceptedURLs←1 2⍴'*' 1
     h.onHTTPRequest←'OnHTTPRequest'
     ⍵.Context←⊃⎕RSI
     h⊣h SetDocument ⍵
 }
