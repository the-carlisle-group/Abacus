 ExecuteOnElementSync←{
     ⍝ ⍺ ←→ Element
     ⍝ ⍵ ←→ Javascript
     ⍝ ← ←→ Result
     i←TagIndex ⍺
     t←⍺.Tag
     c←'document.getElementsByTagName'
     c,←'("',t,'").item(',(⍕i),').',⍵,';'
     c←'''',c,''''
     j←'execCode(',(⍕⎕TID),',',c,')'
     _←⍺.Document ExecuteJavaScript j
     ⎕TGET ⎕TID
 }
