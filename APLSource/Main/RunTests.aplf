 RunTests←{
     ⍝ ⍺ ←→ HTMLRenderer
     ⍝ ⍵ ←→ Namespace of tests
     ⎕TID=0:⍺ ∇&⍵
     h s←⍺ ⍵
     c←'Passed' 'Failed' 'Broken' 'N/A' 'Disabled'
     r←{
         b←{0::2 ⋄ ⎕TSYNC(s⍎⍵)&h}⍵
         b⊣⎕←⍵,': ',⍕b⊃c
     }¨'T's.⎕NL ¯3
     n←(⊣,+/)¯1+{≢⍵}⌸r,⍨⍳≢c
     ⍉↑(c,⊂'Total')n
 }
