 SetDocument←{
     ⍝ ⍺ ←→ HTMLRenderer
     ⍝ ⍵ ←→ Document [Target temp file name]
     d f←2↑⍵,⊂''
     d=0:0
     ⍵.Script←JavaScript
     _←SetParent ⍵
     e←Elements ⍵
     e.Document←⍵
     ⍺.Document←⍵
     ⍵.StopPropagation←0
     ⍵.HTMLRenderer←⍺
     h←DOM2HTML ⍵
     0=≢f:h⊣⍺.HTML←h
     _←h ⎕NPUT f 1
     ⍺.URL←f
     h
 }
