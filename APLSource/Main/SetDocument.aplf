 SetDocument←{
     ⍝ ⍺ ←→ HTMLRenderer
     ⍝ ⍵ ←→ Document
     ⍵=0:0
     ⍵.Script←JavaScript
     _←SetParent ⍵
     e←Elements ⍵
     e.Document←⍵
     ⍺.Document←⍵
     ⍵.StopPropagation←0
     ⍵.HTMLRenderer←⍺
     h←DOM2HTML ⍵
     ⍺.HTML←h
     h
 }
