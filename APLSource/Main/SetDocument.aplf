 SetDocument←{
     ⍝ ⍺ ←→ HTMLRenderer
     ⍝ ⍵ ←→ Document
     ⍵=0:0
     ⍵.Script←JavaScript
     _←SetParent ⍵
     e←Elements ⍵
     e.Document←⍵
     ⍺.Document←⍵
     ⍵.StopPropagation←0
     ⍵.HTMLRenderer←⍺
     ⍺.HTML←DOM2HTML ⍵
     0
 }
