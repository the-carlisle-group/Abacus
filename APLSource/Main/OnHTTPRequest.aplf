 OnHTTPRequest←{
     u←7⊃⍵
     u≡'http://dyalog_root/sjs':OnHTTPRequestSJS ⍵
     u≢'http://dyalog_root/gui':⍵
     h←⊃⍵
     d←h.Document
     q←d.StopPropagation
     d.StopPropagation←0
     q:Return ⍵
     c←⎕JSON 9⊃⍵
     c.HTMLRenderer←h
     j←c.TargetIndex
     k←c.CurrentTargetIndex
     te ce←(Elements d)[j k]
     c.Target←te
     c.CurrentTarget←ce
     ⍝ _←DisplayEventMessage c
     _←(⍎ce⍎'On',c.Event)c
     _←c ⎕TPUT{⍵×⍵∊⎕TREQ ⎕TNUMS}EventToken
     Return ⍵
 }
