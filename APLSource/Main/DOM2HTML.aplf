 DOM2HTML←{
     ⍝ ⍵ ←→ DOM
     ⍝ ← ←→ HTML
     0<⍴⍴⍵:⊃,/∇¨⍵
     _←InjectStyleElement ⍵
     _←InjectScriptElement ⍵
     m←AddXMLSpace↑,ConstructQXI ⍵
     t←RemoveXMLSpace ⎕XML 4↑[1]m
     t←t InjectHTML m[;4]~0
     ⍵.Tag≢'html':t
     t←⍵ InjectStyle t
     t←⍵ InjectScript t
     '<!DOCTYPE html>',(⎕UCS 13),t
 }
