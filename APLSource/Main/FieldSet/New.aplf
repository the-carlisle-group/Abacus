 New←{
     ⍺←0
     (p n)←A.DefaultParentAndName ⍺
     d←(p A.New'fieldset')⎕NS(
         'Content' ''
         'Legend' ''
         'Format' 'stacked'     ⍝ stacked|inline|inline-row|stacked-row inputs
         'Border' 1             ⍝ 0|1
         'Disabled' 0
     )A.Default ⍵
     d.Name←n
     d.Content←d.Legend{0=≢⍺:⍵ ⋄ ⍵,⍨A.New'legend'⍺}d.Content
     c←'fieldset','-',d.Format
     c,←d.Border/' fieldset-border'
     d.class←c
     d
 }
