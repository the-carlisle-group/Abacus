 New←{
     ⍺←0
     d←(⍺ A.New'fieldset')⎕NS(
         'Content' ''
         'Legend' ''
         'Format' 'stacked'     ⍝ stacked|inline|inline-row|stacked-row inputs
         'Border' 0             ⍝ 0|1
     )A.Default ⍵
     l←A.New'legend'd.Legend

     ⍝ d.Content,←l/⍨0≠≢d.Legend
     d.Content←(l/⍨0≠≢d.Legend),d.Content

     c←'fieldset','-',d.Format
     c,←d.Border/' fieldset-border'
     d.class←c
     d
 }
