 InitTestSuite←{
     ⍝ ⍺ ←→ Test Suite Namespace
     ⍝ ⍵ ←→ Document
     ⍺.A←⎕THIS
     ⍺.Document←⍵
     ⍺.Element←⍵
     fx←{⍺.⎕FX,⊂⍵,'←{⍺←Element ⋄ ⍺ A.',⍵,' ⍵}'}
     fx1←{⍺.⎕FX,⊂⍵,'←{⍺←Document ⋄ ⍺ A.',⍵,' ⍵}'}
     _←⍺ fx¨'Press' 'Enter' 'GetDOM'
     _←⍺ fx1'Click'
     fx2←{⍺.⎕FX,⊂⍵,'←{⎕THIS.Element←Document A.',⍵,' ⍵}'}
     _←⍺ fx2'GetElementById'
     0
 }
