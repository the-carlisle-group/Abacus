 Click←{
     ⍝ ⍺ ←→ document
     ⍝ ⍵ ←→ id|Name
     ⍝ ⍵ ←→ Element
     ⍝ ⍵ ←→ Element (clientY clientX offsetX offsetY)
     ~IsString ⍵:(0⊃⍵)MouseDownAt(1⊃⍵)
     a←⌽Elements ⍺
     o←,↓⍉↑a ⎕VGET('id' '')('Name' '')
     e←(o⍳⊂⍵)⊃a,a
     b←2=e.⎕NC'Onclick'
     v←b⊃'mousedown' 'click'
     ⍺.Waiting←1
     _←e DispatchEvent v
     0⊣⎕TGET DoneToken
 }
