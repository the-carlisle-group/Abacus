 NewForm←{
     ⍝ ⍵ ←→ Document
     p←⊂'HTMLRenderer'
     p,←⊂'Coord' 'RealPixel'
     p,←⊂'Size'⍵.Size
     n←NextFormName 0
     h←⍎n ⎕WC p
     h.InterceptedURLs←1 2⍴'*' 1
     h.onHTTPRequest←'OnHTTPRequest'
     h.Caption←⍵.Caption
     ⍝ ⍵.Context←⊃⎕RSI
     h⊣h SetDocument ⍵
 }
