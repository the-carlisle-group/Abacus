 ConstructQXI←{
     ⍝ ⍵ ←→ DOM
     ⍝ ← ←→ ⎕XML input as v/v
     ⍺←0
     IsString ⍵:⊂⍺''⍵(0 2⍴⊂'')
     a←ComposeAttributes ⍵
     c←Condense ⍵.Content
     IsString c:⊂⍺ ⍵.Type c a
     (⊂⍺ ⍵.Type''a),⊃,/(⍺+1)∇¨c
 }
