 ConstructQXI←{
     ⍝ ⍵ ←→ DOM
     ⍝ ← ←→ ⎕XML input as v/v
     ⍺←0
     IsString ⍵:⊂⍺''⍵(0 2⍴⊂'')0
     a←APLCallbackAttributes ⍵
     a←a⍪ComposeAttributes ⍵
     3=≡⍵.Content:⊂⍺ ⍵.Type'⍃'a(⊃⊃⍵.Content)
     c←Condense ⍵.Content
     IsString c:⊂⍺ ⍵.Type c a 0
     (⊂⍺ ⍵.Type''a 0),⊃,/(⍺+1)∇¨c
 }
