 ConstructQXI←{
     ⍝ ⍵ ←→ DOM
     ⍝ ← ←→ ⎕XML input as v/v
     ⍺←0
     ⍵.Type≡'':⊂⍺''⍵.Text(0 2⍴⊂'')
     a←ComposeAttributes ⍵
     c←⍵.Content
     0=≢c:⊂⍺ ⍵.Type''a
     c.Type≡,⊂'':⊂⍺ ⍵.Type(⊃c.Text)a
     r←⊂⍺ ⍵.Type''a
     r,⊃,/(⍺+1)∇¨c
 }
