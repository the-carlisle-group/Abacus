 NewHTMLRenderer←{
     ⍝ ⍺ ←→ [Parent Form]
     ⍝ ⍵ ←→ Document
     OBSOLETE
     ⍺←⎕THIS
     c←⍺≠⎕THIS
     p←⊂'HTMLRenderer'
     p,←⊂'AsChild'c
     p,←⊂'Coord' 'RealPixel'
     p,←⊂'Size'⍵.Size
     h←⍎'HTMLControl'⍺.⎕WC p
     h.InterceptedURLs←1 2⍴'*' 1
     h.onHTTPRequest←'OnHTTPRequest'
     ⍵.Context←⊃⎕RSI
     h⊣h SetDocument ⍵
 }
