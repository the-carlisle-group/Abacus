 NewHTMLRenderer←{
     ⍝ ⍺ ←→ [Parent Form]
     ⍝ ⍵ ←→ Document
     ⍺←⎕THIS
     c←⍺≠⎕THIS
     p←⊂'HTMLRenderer'
     p,←⊂'AsChild'c
     h←⍎'HTMLControl'⍺.⎕WC p
     h.InterceptedURLs←1 2⍴'*' 1
     h.onHTTPRequest←'OnHTTPRequest'
     h._Context←⊃⎕RSI
     h⊣h SetDocument ⍵
 }
