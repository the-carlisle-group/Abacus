 InitColumnMask←{
     ⍝ ⍺ ←→ DataGrid
     ⍝ ⍵ ←→ Boolean vec or 1 for all columns 0 for none
     ⍝      Or an ordered list of column names, '' for none.
     t←⍺.Table
     n←t.Columns.Name
     t.ColumnMask←⍵
     i←{(⎕DR ⍵)∊11 83:⍸(≢n)⍴⍵ ⋄ (n⍳⍵)~≢n}⍵
     t.ColumnIndices←i
     0
 }
