 StartEnterMode←{
     ⍝ ⍺ ←→ Starting text
     ⍝ ⍵ ←→ table
     k←⍺.Key
     t←⍵
     ct←(1⊃t.ActiveCell)⊃t.Columns.Type
     ct≡'CheckBox':0
     ct≡'Select':⍺ ShowPickList t
     t.CellMode←1
     i←''CreateInputField t
     i A.FireKeyDownOnInput k
 }
