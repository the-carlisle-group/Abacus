 OnInputBlur←{
     ⎕←'OnInputBlur...'
     i←⍵.CurrentTarget
     ProcessInput i

     ⍝
⍝     i.value≡'InvalidData':GoBackToCell i
⍝     t←i A.GetNearest'table'
⍝     t.CellMode←0
⍝     t.Changed←1
⍝     e←i A.GetNearest'td'
⍝     e.Value←{⍵}i.value               ⍝ Convert to numeric, etc,
⍝     _←e A.SetInnerHTML{⍵}i.value   ⍝ Format the value!
⍝     e A.RemoveClass'in-cell'
 }
