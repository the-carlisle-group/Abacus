 Replace←{
     ⍝ ⍺ ←→ Table
     ⍝ ⍵ ←→ Matrix, Upper left corner index
     t←⍺
     m i←⍵
     r c←i+⍳¨⍴m
     e←⎕NS''
     e.Event←'BeforeCellChange'
     e.Component←t
     e.CellIndex←i
     e.OldValue←t GetCellValues r c
     e.NewValue←m
     A.FireAbacusEvent e:1    ⍝ New Value can change
     _←t SetCellValues e.NewValue i
     _←t SetChanged 0
     _←Refresh t
     e.Event←'CellChange'
     A.FireAbacusEvent e
 }
