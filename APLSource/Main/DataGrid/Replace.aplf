 Replace←{
     ⍝ ⍺ ←→ Table
     ⍝ ⍵ ←→ Matrix, Upper left corner index
     t←⍺
     m i←⍵
     r c←i+⍳¨⍴m
     e←⎕NS''
     e.Event←'BeforeCellChange'
     e.CellIndex←i
     e.OldValue←t GetValues r c
     e.NewValue←m
     t Fire e:1    ⍝ New Value can change
     _←t SetValues e.NewValue i
     t.Changed←1
     _←Refresh t
     e.Event←'CellChange'
     t Fire e
 }
