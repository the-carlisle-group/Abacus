 SetValue←{
     t v←⍺ ⍵
     i j←t.ActiveCell
     i<0:0⊣t.HeaderValues[⊂i j+t.HeaderRowCount 0]←⊂v
     c←j⊃t.Columns
     d←c.Value
     _←{2=≢⍴d:d[i;]←(1⊃⍴d)↑v ⋄ d[i]←⊂v}0
     c.Value←d
     0
 }
