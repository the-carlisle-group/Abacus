 MoveTo←{
     ⍝ Move for FindAndReplace
     ⍝ ⍺ ←→ Table
     ⍝ ⍵ ←→ ActiveCell
     tc←⍺.TableCell
     ⍺.ActiveCell←⍵
     ⍺.Selection←(≡/⍺.Selection)⊃⍺.Selection(2⍴⊂⍵)
     ⍵ AdjustWindow ⍺:Refresh ⍺
     lc←tc GetTableCell ⍺
     nc←GetTableCell ⍺
     _←⍺ RefreshActiveCellClass lc nc
     0
 }
