 ReplaceCellValues←{
     ⍝ ⍺ ←→ DataGrid
     ⍝ ⍵ ←→ Matrix, upperleft corner
     v i←⍵
     r c←i
     g←⍺
     t←⍺.Table
     ci←{##.IsString ⍵:t.Columns.Name⍳⊂⍵ ⋄ ⍵}c
     _←t SetCellValues v(r ci)
     0⊣Focus Refresh t
 }
