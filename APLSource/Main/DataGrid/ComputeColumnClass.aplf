 ComputeColumnClass←{
     ⍝ r Row Number
     ⍝ n Numeric
     ⍝ d Date
     ⍝ c Char
     ⍝ b CheckBox
     ⍝ s Select
     ⍝ m MultiSelect
     ⍝ v Varchar (todo)
     ⍝ o Object  (todo)
     t←⍵
     m←ColumnTypeInfo 0
     c←m[m[;0]⍳t.Columns.Type;1]
     (t.RowNumbers/'r'),c
 }
