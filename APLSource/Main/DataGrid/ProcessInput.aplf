 ProcessInput←{
     ⎕←'ProcessInput'
     i←⍵
     ev←⍵.value   ⍝ Entered Value, char
     ev≡'Crap':1  ⍝ Set focus back on input
     pv←ev        ⍝ Processed value (char, num, date...)
     fv←ev        ⍝ Formatted value (char)
     t←i A.GetNearest'table'
     t.CellMode←0
     t.Changed←1
     t.TableData.Values[⊂t.DataCell]←⊂pv  ⍝ For now
     e←i A.GetNearest'td'
     _←i A.RemoveAttribute'onblur'
     _←e A.SetInnerHTML fv
     _←e A.RemoveClass'in-cell'
     0
 }
