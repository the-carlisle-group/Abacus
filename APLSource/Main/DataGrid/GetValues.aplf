 GetValues←{
     ⍝ ⍺ ←→ Table
     ⍝ ⍵ ←→ Row indices, column indices
     ⍝ ← ←→ Matrix of values
     v←⍺ IndexValues,¨⍵
     s←0=(≢⍴)¨⍵
     m←⍉↑{2=≢⍴⍵:ntbs ⍵ ⋄ ⍵}¨v
     ∧/s:⊃m
     1∊s:,m
     m
 }
