 Sort←{
     t←⍺
     t.InsertRows:t.Document A.Alert'Sorting is not supported for DataGrids that allow row insertion.'
     j k←t.ActiveCell
     c←k⊃t.Columns
     s←×c.SortDirection
     d←1 ¯1 0[¯1 0 1⍳s]
     c.SortDirection←d×1+⌈/|t.Columns.SortDirection
     t.RowIndices←t ReSort t.RowIndices
     t.HeaderClass←ComputeHeaderClass t
     Refresh t
 }
