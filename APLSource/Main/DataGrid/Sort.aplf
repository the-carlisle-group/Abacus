 Sort←{
     ⍝ ⍺ ←→ Table
     ⍝ ⍵ ←→ Sort Direction ¯1, 0, 1
     ~⍺.SortRows:0
     ⍺.ReadOnly:⍺ SortReadOnly ⍵
     j k←⍺.ActiveCell
     c←⍺.Columns
     c.SortDirection←⍵×k=⍳≢c
     g←⍺ ReSort ⍺.RowIndices
     c.Value←g∘(⊂⍛⌷)¨c.Value
     c.HeaderClass←ComputeHeaderClass c
     Focus Refresh ⍺
 }
