 PropList←{
     p←⊂'Values'          ⍝ Required (mat or inverted)
     p,←⊂'Names'          ⍝ Corresponding to Values
     p,←⊂'Types'          ⍝ Correponding to Values
     p,←⊂'ReadOnlyColumns'⍝ Boolean vector corresponding to 1⊃⍴Values
     p,←⊂'ReadOnlyRows'   ⍝   ''                            0⊃⍴Values
     p,←⊂'ReadOnly'       ⍝
     p,←⊂'RowNumbers'     ⍝ Display row numbers. 1|0.
     p,←⊂'FontSize'       ⍝ Pixels. Defaults to 18
     p,←⊂'InsertRows'     ⍝ Allow insert (and delete, move) rows.
     p,←⊂'InsertColumns'  ⍝ Allow inserting (and delete, move, rename) columns.
     p,←⊂'FreezeColumns'  ⍝ Number of left-most columns to freeze.
     ⍝ Read-only Properties:
     p,←⊂'Changed'        ⍝ Has Values property been changed.
     p,←⊂'WindowIndex'    ⍝ Index of upper left corner
     p,←⊂'DataCell'       ⍝ Current Cell (should probably rename this)
     p
 }
