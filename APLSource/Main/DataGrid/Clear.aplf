 Clear←{
     ⍝ ⍵ ←→ 0 for light, 1 for strong
     ⍝ ⍺ ←→ DataGrid
     ⍵:0⊣⍺.Table←⍺ NewTable''
     t←⍺.Table
     t.Init←0
     _←t.⎕EX'HeaderValues'
     _←t.⎕EX'WindowIndex'
     _←t.⎕EX'ActiveCell'
     _←t.⎕EX'Columns'
     0
 }
