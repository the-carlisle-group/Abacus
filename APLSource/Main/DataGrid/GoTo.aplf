 GoTo←{
     ⍝ ⍺ ←→Table
     ⍝ ⍵ ←→ DataCell
     HandleInput ⍺:0
     _←ClearSelection ⍺
     tc←⍺.TableCell
     ⍝ (⊃⍵)=⊃⍵.DataSize:AppendRow ⍵
     ∨/(⍵<0)∨⍵≥⍺.DataSize:0
     ⍺.DataCell←⍵
     ⍺.Selection←⍵
     ⍵ AdjustWindow ⍺:Refresh ⍺
     bc←A.BodyCells ⍺
     lc←tc⌷bc
     nc←⍺.TableCell⌷bc
     _←lc nc A.MoveClass'window-cell'
     _←⍺ A.FireEvent'focus'
     0
 }
