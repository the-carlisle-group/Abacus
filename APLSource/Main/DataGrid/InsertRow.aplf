 InsertRow←{
     t←⍵
     t.ReadOnly:0
     ~t.CanInsertRows:0
     HandleInput t:0
     i j←t.ActiveCell
     n m←t.DataSize
     e←(0@i)1⍴⍨n+1
     c←t.Columns
     c.Value←e∘⍀¨c.Value
     c.Value←c.Value{v←⍺
         1=≢⍴v:v⊣v[i]←⊂⍵
         v⊣v[i;]←(1⊃⍴v)↑⍵
     }¨c.Default
     t.DataSize+←1 0
     t.RowIndices←⍳⊃t.DataSize
     _←t SetChanged 0
     t.Parent Resize 0
 }
