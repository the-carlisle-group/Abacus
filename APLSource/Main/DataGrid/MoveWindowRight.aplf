 MoveWindowRight←{
     t←⍵
     ⍝ Cant use DataCell, as might be moving to select a range
     ⍝ and DataCell is fixed
     ⍝ SHould we have yet another Var to track? CursorCell?
     ⍝ c←1⊃t.DataCell
     c←1⊃t.WindowIndex+t.WindowSize

     i←c⊃t.ScrollIntoViewIndex
     t.WindowIndex[1]←i
     t.WindowSize[1]←i⊃t.ColumnsPerIndex
     t.WindowCell[1]←c-i        ⍝ t.DataCell-t.WindowIndex
     t.TableCell←t.WindowCell+0,t.ColumnOffset
     _←Refresh t
     0
 }
