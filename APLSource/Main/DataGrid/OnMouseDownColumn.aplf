 OnMouseDownColumn←{
     ⍝ Click on Column Header
     ⍝ Make first cell of column active
     e←⍵.Target         ⍝ Know it's th
     t←⍵.CurrentTarget
     t.SelectType←'Column'
     c←t.HeaderCells
     i←e.Index
     t.TableCell←0,i
     t.WindowCell←t.TableCell-0,t.FixedColumns
     t.ActiveCell←t.WindowCell+t.WindowIndex
     n m←t.DataSize-1
     t.Selection←t.ActiveCell(n,1⊃t.ActiveCell)
     _←Refresh t
     0
 }
