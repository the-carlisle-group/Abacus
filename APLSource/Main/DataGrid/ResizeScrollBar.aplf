 ResizeScrollBar←{
     ⍝ ⍺ ←→ Scrollbar
     ⍺.ContentLength←0⊃⍵
     ⍺.TrackLength←1⊃⍵
     ⍺.ThumbLength←20⌈⍺.TrackLength⌊⍺.TrackLength{⍺×⍺÷⍵}1⌈⍺.ContentLength
     ⍺.MaxTopLeft←⍺.TrackLength-⍺.ThumbLength
     th←⊃⍺.Content
     p←⍺.Vert⊃'width' 'height'
     _←th A.SetStyle p('px',⍨⍕⍺.ThumbLength)
     0
 }
