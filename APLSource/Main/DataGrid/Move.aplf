 Move←{
     ⍝ ⍺ ←→Table
     ⍝ ⍵  ←→ ActiveCell
     HandleInput ⍺:0
     2≠⍺.⎕NC'TableCell':⍺{∨/⍵<0:0⋄⍺.(ActiveCell Selection)←⍵(2⍴⊂⍵)⋄0}⍵ ⍝ grid not rendered yet: store a valid (non-negative) cell so Resize/GetContent shows it; ignore header/negative targets (can't nav pre-render)
     _←ClearSelection ⍺
     tc←⍺.TableCell
     0>0⊃⍵:⍺ GoToHeaderRow ⍵
     ∨/(⍵<0)∨⍵≥⍺.DataSize:0
     e←⎕NS''
     e.Event←'CellMove'
     e.Component←⍺.Parent
     e.OldCellIndex←⍺.ActiveCell
     e.NewCellIndex←⍵
     ⍺.ActiveCell←⍵
     ⍺.Selection←2⍴⊂⍵
     f←⍺.OnCellMove
     ⍵ AdjustWindow ⍺:f A.FireAbacusEvent e⊣Refresh ⍺
     lc←tc GetTableCell ⍺
     nc←GetTableCell ⍺
     _←⍺ RefreshActiveCellClass lc nc
     _←f A.FireAbacusEvent e
     0
 }
