 Move←{
     ⍝ ⍺ ←→Table
     ⍝ ⍵ ←→ DataCell
     HandleInput ⍺:0
     _←ClearSelection ⍺
     tc←⍺.TableCell
     (⊃⍵)=⊃⍺.DataSize:AppendRow ⍺
     ∨/(⍵<0)∨⍵≥⍺.DataSize:0
     e←⎕NS''
     e.Event←'CellMove'
     e.OldCellIndex←⍺.DataCell
     e.NewCellIndex←⍵
     ⍺.DataCell←⍵
     ⍺.Selection←⍵
     ⍵ AdjustWindow ⍺:e Refresh ⍺
     bc←⍺.BodyCells
     lc←tc⌷bc
     nc←⍺.TableCell⌷bc
     _←lc nc A.MoveClass'window-cell'
     _←nc A.FireEvent'focus'
     _←⍺ Fire e
     0
 }
