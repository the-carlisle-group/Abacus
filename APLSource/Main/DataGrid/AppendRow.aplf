 AppendRow←{
     t←⍵
     t.ReadOnly:0
     ~t.InsertRows:0
     c←t.Columns
     c.Value←c.Value{1=≢⍴⍺:⍺,⊂⍵ ⋄ ⍺⍪(1⊃⍴⍺)↑⍵}¨c.Default
     t.RowIndices,←⊃t.DataSize
     t.DataSize+←1 0
     t.MaxWindowIndex+←1 0
     _←t Move t.ActiveCell+1 0
     0⊣Focus Refresh t
 }
