 AppendRow←{
     t←⍵
     t.ReadOnly:0
     ~t.InsertRows:0
     i j←t.ActiveCell
     n m←t.DataSize
     c←t.Columns
     c.Value←c.Value{1=≢⍴⍺:⍺,⊂⍵ ⋄ ⍺⍪(1⊃⍴⍺)↑⍵}¨c.Default
     t.DataSize+←1 0
     t.MaxWindowIndex+←1 0
     t Move t.ActiveCell+1 0
 }
