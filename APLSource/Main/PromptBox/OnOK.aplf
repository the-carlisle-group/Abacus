 OnOK←{
     ⍝ ⍵ ←→ Event message from Dialog Click event
     d←⍵.Component
     c←d A.GetComponent'PromptBox'
     ⍵.Event←'OK'
     ⍵.OldValue←d.Value
     ⍵.NewValue←c.Value
     f←d.OnOK
     0=≢f:0
     (⍎f)⍵
 }
