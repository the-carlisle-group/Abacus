 ExecuteOnElement←{
     ⍝ ⍺ ←→ Element
     ⍝ ⍵ ←→ Javascript
     i←TagIndex ⍺
     t←⍺.Tag
     j←'document.getElementsByTagName'
     j,←'("',t,'").item(',(⍕i),').',⍵,';'
     ⍺.Document.HTMLRenderer.ExecuteJavaScript j
 }
