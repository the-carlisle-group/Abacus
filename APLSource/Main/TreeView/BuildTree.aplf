 BuildTree←{
     ⍝ ⍵ ←→ Items,Depth matrix
     ⍝ ← ←→ <ul>
     ⍺←A.New'ul'
     ⍺.Leaf←0=≢⍵
     ⍺.Leaf:⍺
     ul←⍺ A.New'ul'
     ul._display←'none'
     (i d)←↓⍉⍵
     b←d=⊃d
     li←ul A.New¨{'li'⍵}¨b/i
     ul⊣li ∇¨1↓¨b⊂[0]⍵
 }
