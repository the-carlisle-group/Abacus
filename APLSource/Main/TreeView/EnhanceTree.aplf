 EnhanceTree←{
     ⍝ ⍵ ←→ Result of BuildTree
     ⍝ ← ←→ passed through
     li←⍵ A.GetElementsByTag'li'
     0=≢li:⍵
     li.Index←⍳≢li
     n←(~li.Leaf)/li
     0=≢n:⍵
     n←EnhanceParentNode¨n
     ⍵
 }
