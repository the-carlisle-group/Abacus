 New←{
     ⍺←0
     t←A.InitComp(⍺ A.New'ul')⎕NS(
         'Items' ''
         'Depth'⍬
         'OnSelect'('' 0)
     )A.Default ⍵
     t.OnSelect←(
         'CallbackFunction' ''
         'DebounceDelay' 0
     )A.Default t.OnSelect
     t.ActiveIndex←0
     t.tabindex←0
     t.Onkeydown←A.FQP'OnKeyDown'
     _←Init t
     t
 }
