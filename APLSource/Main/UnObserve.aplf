 UnObserve←{
     ⍝ ⍺ ←→ Document
     ⍝ DOM Elements
     e←∊Elements¨⍵
     f←e ⎕VGET⊂'OnResize' ''
     v←(~f∊⊂'')/e
     0=≢v:0
     ⍺{⍺ ExecuteJavaScript'resizeObserver.unobserve(document.getElementById("',⍵,'"))'}¨v.id
 }
