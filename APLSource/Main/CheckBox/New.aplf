 New←{
     ⍝ Label
     ⍝ Name/id
     ⍝ Value
     c n v←⍵
     A←##
     d←⍺ A.New'div'
     d.class←'check-box'
     i←d A.New'input'
     i.type←'checkbox'
     i.id←n
     l←d A.New'label'c
     l.for←n


     ⍝ i.Onblur←A.FQP'OnInputBlur'
     ⍝ i.Oninput←A.FQP'OnInput'
     ⍝ i.Onkeydown←A.FQP'OnKeyDown'
     ⍝ i.value←v
     ⍝ i.CurrentValue←v
     ⍝ i.OriginalValue←v
     d
 }
