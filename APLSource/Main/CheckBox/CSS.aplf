 CSS←{
     Rule←A.NewRule
     ⍝ ************************
     s←Rule'.CheckBox'
     s.display←'flex'
     s.align_items←'center'
     s.gap←'.5em'
     ⍝ *******************************
     r←s Rule'input'
     r.width←'1.25rem'
     r.height←'1.25rem'
     r.accent_color←'steelblue'
     ⍝ ***************************
     A.UnNestCSS s
 }
