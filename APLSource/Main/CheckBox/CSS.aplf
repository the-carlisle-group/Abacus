 CSS←{
     Rule←A.NewRule
     ⍝ ************************
     s←Rule'.CheckBox'
     s.display←'flex'
     s.gap←'.5rem'
     ⍝ *******************************
     r←s Rule'input'
     r.width←'1rem'
     r.height←'1rem'
     r.accent_color←'steelblue'
     ⍝ ***************************
     A.UnNestCSS s
 }
