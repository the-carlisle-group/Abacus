 MoveCol←{
     ⍝ ⍺ ←→ Table
     ⍝ ⍵ ←→ New Location
     m←A.BodyCells ⍺
     c←⍺{3::0 ⋄ m[0;⍺.CurCol ⍵]}⍵
     c≡0:0
     oc nc←c
     oc=nc:0
     _←nc A.AddClass'curcol'
     _←oc A.RemoveClass'curcol'

     h←A.HeaderCells ⍺
     th←⍺{3::0 ⋄ h[0;⍺.CurCol ⍵]}⍵
     oh nh←th
     _←nh A.AddClass'curheader'
     _←oh A.RemoveClass'curheader'


     ⍺.CurCol←⍵
     _←A.ScrollIntoView nh ⍝ 1⊃c
     0
 }
