 New←{
     ⍺←0
     (p n)←A.DefaultParentAndName ⍺
     b←A.InitComp(p A.New'button')⎕NS(
         'Caption' ''
         'Icon' 'none'
         'OnClick' ''
         'Disabled' 0
     )A.Default ⍵
     b.tabindex←'-1'
     b.Name←n
     b.OnClick←1 A.FQP b.OnClick
     i←(##.Icons 0)⎕VGET⊂b.Icon''
     b.Content,←(0<≢i)/##.New'div'(⊂⊂i)  ⍝ Need div only because DOM2HTML does not handle ...
     b.Content,←(0<≢b.Caption)/##.New'p'b.Caption
     b
 }
