 Arrange_Orig←{
     ⍝ ⍺ ←→ grid element
     ⍝ ⍵ ←→ Arrangement matrix
     g←⍺
     c←g.Content
     m←{1 3∊⍨10|⎕DR ⍵:⍵ ⋄ c.id⍳⊆⍵}⍵
     u←∪,m
     b←~u∊⍨⍳≢c
     _←c SetDisplayNone¨b
     i←(⊂⍳2)+1+(⊣/,⊢/)¨⍸¨⊂[⍳2]m∘.=u
     a←{'/'@(' '∘=)⍕∊⍵}¨i
     f←{'repeat(',(⍕⍵),', 1fr)'}
     _←g SetStyle'gridTemplateRows'(f≢m)
     _←g SetStyle'gridTemplateColumns'(f 1⊃⍴m)
     _←c[u]SetStyle¨{'gridArea'⍵}¨a
     0
 }
