 SetComponentValue←{
     ⍝ ⍺ ←→ Component or  Document  Name
     ⍝ ⍵ ←→ Value

     ⍝ 1=≢⍺:⍺ ⍺.Class.SetValue ⍵
     ⍝ d n←⍺

     c←GetComponent/⍺
     c c.Class.SetValue ⍵
 }
