 ConstructQuadXMLInput←{
     ⍺←0
     ⍵.Type≡'':⊂⍺''⍵.Text(0 2⍴⊂'')
     c←,⊃Condense/⍵.Content
     ⍵.Content←c
     a←ComposeAttributes ⍵
     c.Type≡,⊂'':⊂⍺ ⍵.Type(⊃c.Text)a
     r←⊂⍺ ⍵.Type''a
     r,⊃,/(⍺+1)∇¨c
 }
