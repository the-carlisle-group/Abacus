 SetComponentValues←{
     ⍝ ⍺ ←→ DOM node
     ⍝ ⍵ ←→ Name/Value paris, or Namespace of values
     e←⍺ GetElementsWith'Name'
     0=≢e:0
     m←e.Name
     n v←↓⍉↑{9=⎕NC'⍵':⍵ ⎕VGET ¯2 ⋄ ⍵}⍵
     i j←m n⍳¨⊂m∩n
     0=≢i:0
     c←e[i].Class
     0⊣e[i]c.SetValue v[j]
 }
