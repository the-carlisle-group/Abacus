 GetStartText←{
     ⍝ ⍵ ←→ Event Mesage
     ⍝ ← ←→ Return code, starting text
     t←⍵.Value
     l←⍵.Target.AutoCompleteElement
     k←⍵.Key
     bs←k≡'Backspace'
     s←⍵.SelectionStart
     st et←(t↑⍨s-bs)(s↓t)
     tk←l.TriggerKeys
     tk∊⍨⊃et:1 ''
     r←(⌽∧\⌽st∊tk)/st
     0(r,k⍴⍨~bs)
 }
