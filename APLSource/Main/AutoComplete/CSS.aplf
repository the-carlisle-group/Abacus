 CSS←{
     Rule←##.NewRule
     s←Rule'.auto-complete'
     s.background_color←'lightyellow'
     s.max_height←'15rem'
     s.position←'absolute'
     s.overflow←'auto'
     r←s Rule'div'
     r.padding←'.1rem .2rem'
     r←s Rule'.active-item'
     r.color←'white'
     r.background_color←'blue'
     ##.UnNestCSS s
 }
