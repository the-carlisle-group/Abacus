 InitContent←{
     ⍝ ⍺ ←→ Toolbar instance
     ⍝ ⍵ ←→ Content
     d←⍺
     d.Content←⍵
     0=≢⍵:0
     e←A.GetElements d
     d.FocusContent←e/⍨e.Tag∊'button' 'select' 'input'
     d.FocusContent.tabindex←⊂'-1'
     f←⊃d.FocusContent
     f.class,←' Active'
     0
 }
