 SelectIt←{
     ⍝ ⍵ ←→ Event Message
     p←⍵.CurrentTarget
     i←p.Content.class⍳⊂'sel-item'
     p.Value←i⊃p.Items
     f←(⍎p.OnSelect)⍵
     p A.ExecuteOnElement'hidePopover()'
     A.DeleteElement p
 }
