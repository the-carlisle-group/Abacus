 ReArrange←{
     g←⍺
     g.GridTemplateAreas←⍵
     _←Init g
     c←g.Content
     _←c A.SetDisplayNone¨g.Hidden
     ⍝ Can we set style in one go?
     ⍝ Is setting style cumulative? Or does it override?
     ⍝ We have already set style with padding and gap.
     _←g A.SetStyle'gridTemplateRows'g.GTR
     _←g A.SetStyle'gridTemplateColumns'g.GTC
     _←c[⍸~g.Hidden]A.SetStyle¨{'gridArea'⍵}¨g.GridAreas
     0
 }
