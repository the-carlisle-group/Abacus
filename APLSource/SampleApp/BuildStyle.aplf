 BuildStyle←{
     s←⍬
     ⍝ Body
     s,←r←H.NewRule'body'
     r.display←'flex'
     r.min_height←'97vh'
     r.max_height←'97vh'
     r.flex_direction←'column'
     ⍝ Header
     s,←r←H.NewRule'body>header'
     r.background_color←'gray'
     ⍝ Main
     s,←r←H.NewRule'body>main'
     r.flex←1
     r.background_color←'lightgray'
     r.overflow←'auto'
     ⍝ Footer
     s,←r←H.NewRule'body>footer'
     r.background_color←'gray'
     s
 }
