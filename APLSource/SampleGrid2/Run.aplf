 Run←{
     ⍺←10 3
     r c←⍺
     ⎕THIS.A←#.Abacus.Main
     ⍝h←'This is Column '∘,¨⍕¨⍳c
     h←c⍴'Short' 'This is a really long header' 'Short again'
     ⍝ b←⍕¨100 50⍴⍳100×50
     b←⍕¨r c⍴⍳r×c
     t←A.NewTable b h
     t.Width←c⍴'10rem' '5rem' '15rem'  ⍝ Compute
     t.TextAlign←c⍴'Left' 'Right' 'Right' ⍝ Compute
     g←##.Grid.New t
     d←A.NewDocument g
     d.Style←''
     d.Caption←'Grid Sample'
     A.NewForm d
 }
