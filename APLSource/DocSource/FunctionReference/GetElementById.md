# GetElementById

Test function.

This is a function injected to a test suite.

~~~
R←GetElementById X
~~~

`X` is an id. R is an element from the APLDOM.

The returned element is also globally specified in the 
test space as `Element`, indicating the current element.

See also [GetElementFromBrowser]().
