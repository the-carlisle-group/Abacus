# DataGrid FilterColumns Propety

Boolean scalar. Determines if the user may select and reorder the displayed columns.

Only applicable if [ReadOnly]() is `1`.

The [DisplayColumns]() property and [SetDisplayColumns]() method may be applied
regardless of the FilterColumns property.
