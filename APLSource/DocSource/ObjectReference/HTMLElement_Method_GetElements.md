# HTMLElement GetElements Method

Gets a vector of all sub elements. 

~~~
      R←X GetElements 0
      R←GetElements X
~~~

`X` is an HTMLElement. `R` is a vector of `X` and all of its sub elements.

