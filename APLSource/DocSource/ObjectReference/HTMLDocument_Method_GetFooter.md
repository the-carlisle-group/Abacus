# HTMLDocument GetFooter Method

Returns the first (top-level) `<footer>` element of a document.

~~~
    R←I GetFooter 0
    R←GetFooter I
~~~

`I` is an HTMLDocument instance.
`R` is the <footer> element. 

