# Application Build Event

An callback function must be specified for this event, and it must return an
[HTMLDocument]() object.

This event occurs once for a desktop application, and once
for every new session in a web application. 
