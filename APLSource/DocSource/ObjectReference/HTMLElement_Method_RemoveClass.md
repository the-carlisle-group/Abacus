# HTMLElement RemoveClass Method

Removes a class from an element. 

~~~
      R←I RemoveClass X
~~~

> This is an APLDOM to browser DOM synchronized method.

`I` is the HTMLElement instance. `X` is a class name.
`R` is 0.

