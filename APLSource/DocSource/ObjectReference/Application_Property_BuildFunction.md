# Application BuildFunction Property

The `BuildFunction` property specifies the name of the function
that builds the application. This function must return an
[HTMLDocument]() object.

This function will run once for a desktop application, and once
for every new session in a web application. 
