# Application Object


The property specfies a function to run on starting a desktop application,
or starting a new session in a web application.

The function runs after the `BuildFunction` has created the web page, and after
a websocket connection has been established.
