# NumberInput Format Property                 

A format string suitable for `⎕FMT`.

The format string defines the display format,
but also the precision of what is accepted as
input and stored. 

