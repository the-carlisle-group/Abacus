# ProgressBar LanguageTable Property

Specifies the labels for the various UI elements. The default is:

~~~
     [
         'Pause' 'Pause'
         'Cancel' 'Cancel'
         'Truncate' 'Truncate'
         'Resume' 'Resume'
         'Paused' 'Paused'
     ]
~~~
