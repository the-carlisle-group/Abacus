# HTMLElement Parent Property

The `Parent` property returns the parent HTMLElement.
HTMLElements may not always have a parent, as they
can exist in isolation, not associated with any document.
In this case the `Parent` property is `0`. 

