# HTMLElement SetComponentValues Method

Sets the Value property for components under an element.

~~~
   R←I SetComponentValues X
~~~

`I` is an HTMLElement instance. `X` is a namespace of values. R is 0.


