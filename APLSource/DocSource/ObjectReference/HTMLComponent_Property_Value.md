# HTMLComponent Value Property

The `Value` property returns the value of the component.

To set the value, use the [SetComponentValue]() method.

See the `Value` property for each component for a detailed
description.
