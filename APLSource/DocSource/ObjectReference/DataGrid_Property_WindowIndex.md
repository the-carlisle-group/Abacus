# DataGrid WindowIndex Property

Specifies the cell that should occupy the upper left corner of the grid.
Defaults to 0 0.

The [SetWindowIndex]() method should be used after the grid has been initialized. 
