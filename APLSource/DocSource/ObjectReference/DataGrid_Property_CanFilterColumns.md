# DataGrid CanFilterColumns Propety
                  
Boolean scalar. Determines if the user may select and reorder the displayed columns.

Only applicable if [ReadOnly]() is `1`.

The [ColumnMask]() property and [SetColumnMask]() method may be applied
regardless of the `CanFilterColumns` property.
