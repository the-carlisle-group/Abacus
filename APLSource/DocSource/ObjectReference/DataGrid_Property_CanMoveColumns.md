# DataGrid CanMoveColumns Property

Boolean scalar. Allow or disallow moving columns. The default is `1`.

This property only applies if ReadOnly is `0`.
If ReadOnly is `1`, columns may not be moved.
