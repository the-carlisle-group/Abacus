# HTMLDocument GetHeader Method

Returns the first (top-level) `<header>` element of a1 document.

~~~
    R←I GetHeader 0
    R←GetHeader I
~~~

`I` is an HTMLDocument instance.
`R` is the <header> element. 

