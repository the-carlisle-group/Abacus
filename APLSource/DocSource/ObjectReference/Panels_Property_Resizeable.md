# Panels Resizeable Property

Boolean scalar.
Determines if the grid items are resizeable or not. The default is `0`.
If resizeable, the grid items are focusable, the panels may be resized
using Alt with arrow keys.
