# ProgressBar Exit Property

Boolean. Specifies if the [Run]() method should terminate.
Should be set to `1` by the iterator function to indicate the process is complete and
no more iterations are necessary.
