# HTMLDocument LogBrowserEvents Property

Boolean. Specify if browser events that are sent to Abacus should be logged to the session.

To cut down on session noise when logging, specific events may be specified via
the [LogBrowserEventList]() property.

For logging Abacus component events see [LogAbacusEvents](). 
