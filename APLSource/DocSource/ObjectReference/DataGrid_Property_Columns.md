# DataGrid Columns Property

The `Columns` property is a vector of [DataGridColumn]() objects,
one for each column.

The [SetColumns]() method should be used for setting this property
after the grid is initialized.
