 OnFileOpenOK←{
     p←A.GetComponentValues ⍵.DialogBox
     f←##.CSVEditorTests.GetFileName p.FileName
     f~←'"'
     (('\'=f)/f)←'/'
     p.FileName←f
     p.Variants←GetVariants p
     ⍝ Does file exist: d A.AlertBox.Show 'File not found.'
     _←⍵.Document Open p
     0
 }
