 Build←{
     d←A.NewApplication''
     b←A.GetBody d
     a←b A.AutoComplete.New''
     d.AutoCompleteElement←a
     h←b A.New'header'
     h1←h A.New'h1' 'filename'
     mb←b BuildMenu''
     m←b A.New'main'
     g←m A.DataGrid.New'' ⍝ Dont even need this here.
     f←b A.New'footer'
     h2←f A.New'h2' 'x rows by n columns'
     d
     ⍝#.FlipDBDesktop.Main.TabEx.BuildMainPage
 }
