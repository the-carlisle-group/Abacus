 ReadDCF←{
     p←⎕NS''
     p.TargetTie←⍵ ⎕FTIE 0
     pb←(
         'Title' 'Reading columns'
         'Status'(⎕FREAD p.TargetTie 1)
     )
     r v←⍺ ReadColumn A.ProgressBar.Run p pb
     _←⎕FUNTIE p.TargetTie
     r(↓⍉↑v)
 }
