 Compose←{
     ⍝ ⍵ ←→ Vector of CSS Rules
     nl←⎕UCS 13
     ⊃,/{
         s←⍵.Selector,' {',nl
         n←(⍵.⎕NL ¯2)~'Selector' 'Media'
         v←⍕¨⍵⍎¨n
         p←'-'@('_'∘=)¨n
         s←s,⊃,/p{'    ',⍺,': ',⍵,';',nl}¨v
         s←s,'}',2⍴nl
         0=≢⍵.Media:s
         '@media ',⍵.Media,' {',nl,s,nl,'}',nl
     }¨⍵
 }
