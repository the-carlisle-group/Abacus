 ReInit←{
     ⍝ ⍺ ←→ Number of rows in grid
     ⍝ ← ←→ <table>
     t←Init GetTable 0
     ⍺←≢(⊃t.Columns).Value
     t.Columns.Value←⍺↑¨t.Columns.Value
     t⊣t.Parent A.DataGrid.SetColumns t.Columns
 }
