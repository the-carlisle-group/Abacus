 ReInitAllDataTypes←{
     t←GetTable 0
     t.ActiveCell←0 0
     t.WindowIndex←0 0
     t.RowNumbers←1
     t.RowMinimum←0
     t.InsertRows←1
     t.InsertColumns←1
     t.ColumnIndices←⍬
     t.RowMask←1
     c←⎕NS¨15⍴⊂''
     t.Columns←c
     c.Name←'CheckBox' 'Select' 'MultiSelect' 'AutoComplete' 'Char' 'VarChar' 'Integer' 'Decimal1' 'Decimal2' 'Decimal3' 'Float' 'Date' 'FormattedFloat' 'CustomType' 'RGB'
     c.Type←'CheckBox' 'Select' 'MultiSelect' 'Char' 'Char' 'VarChar' 'Int' 'Dec1' 'Dec2' 'Dec3' 'Float' 'Date' 'Float' 'MyCustomType' 'RGB'
     (1⊃c).Options←'Alabama' 'New Jersey' 'New Hamshire' 'New York' 'California' 'Nevada'
     (2⊃c).Options←'Bacon' 'Ham' 'Sausage' 'Cheese' 'Peppers' 'Onions' 'Spinach'
     (3⊃c).Options←'Bacon' 'Ham' 'Sausage' 'Cheese' 'Peppers' 'Onions' 'Spinach' 'toDate' 'toChar' 'toNumeric'
     ⍝ (4⊃c).OnValidate←'#.Abacus.Tests.DataGridSuite1.OnValidate'
     (2⊃⌽c).FormatString←'F20.2'
     (2⊃⌽c).Null←⎕NULL
     (6⊃c).FormatString←'Q< mb>K¯6CF12.2'
     ⍝ (9⊃c).FormatString←'CF20.5'

     ⍝ Try a custom type
     ct←13⊃c
     ct.OnFormat←'{{1↓∊'','',¨⍵}¨⍵}'
     ct.OnParse←'{1 ('',''(≠⊆⊢),⍵)''''}'
     ct.OnValidate←'{(∧/⍵∊''NY'' ''NJ'' ''CT'')(∪⍵)''Must be in tri-state area.''}'
     ct.Align←'l'

     ⍝ Try a custom type
     ct←14⊃c
     ct.OnFormat←A.FQP'OnFormatRGB'
     ct.OnParse←A.FQP'OnParseRGB'
     ct.OnValidate←'{1 ⍵ ''''}' ⍝ Should not need to define this
     ct.Align←'r'


     n←5
     ⍝ t.RowMinimum←10
     v←⊂n⍴1 0 0
     v,←⊂↑n⍴'Alabama' '' 'Alabama' 'New York' '' 'California'
     v,←⊂↑n⍴'Onions' 'Spinach',n⍴⊂''
     v,←⊂↑n⍴(⊂'This is a really long string'),n⍴⊂''
     v,←⊂↑n⍴'One' 'Two' 'Three' 'Four' 'Five' 'Six' 'Seven' 'Eight' 'Nine'
     v,←⊂n⍴'This is a long string' '' 'A Short one' 'Strings of different length' '' 'Some empty'
     v,←⊂⍳n
     v,←⊂10×⍳n
     v,←⊂100×⍳n
     v,←⊂1000×⍳n
     v,←⊂3÷⍨⍳n
     v,←⊂n⍴20251201+⍳31
     v,←⊂n⍴(3⍴⎕NULL),3÷⍨⍳n
     v,←⊂n⍴''('NY' 'NJ' 'CT')(,⊂'NY')  ⍝ CustomType, a list. 'NY,CA,VT'
     v,←⊂n⍴⍬(0 0 0)(255 255 255)(27 255 156)
     c.Value←v
     t.Init←0
     _←t.Parent A.DataGrid.SetColumns c
     t
 }
