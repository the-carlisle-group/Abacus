 HasClass←{
     ⍝ ⍺←→Element
     ⍝ ⍵←→Class Name(s)
     2=⍺.⎕NC'class':∧/(⊆⍵)∊' '(≠⊆⊢)⍺.class
     0=⍺.⎕NC'Attributes':0
     s←()⎕VSET↓⍺.Attributes
     0=s.⎕NC'class':0
     ∧/(⊆⍵)∊' '(≠⊆⊢)s.class
 }
