 StartInCellMode←{
     ⍝ ⍺ ←→ Starting text
     ⍝ ⍵ ←→ <td> element
     ⍺←⊃⍵.Content
     i←H.New'input'
     i.value←⍺
     i.CurrentValue←⍺
     i.OriginalValue←⊃⍵.Content
     i.Onkeydown←'OnKeyDownInCell'
     i.Onblur←'OnInputBlur'
     i.Parent←⍵
     i.Document←⍵.Document
     ⍵.Content←i
     _←⍵ H.SetProperty'innerHTML'(H.DOM2HTML i)
     n←⍕≢⍺
     _←i H.ExecuteOnElement'setSelectionRange(',n,',',n,')'
     i H.FireEvent'focus'
 }
