 Init←{
     ⍝ ⍺ ←→ Input element, Parent for Autocomplete
     ⍝ ⍵ ←→ Autocomplete Items

     ⍝ To do: Long list, max height, scroll, overflow, etc.


     ⎕THIS.H←##.Main
     p i←⍺
     ai←⍵
     i.autocomplete←'off'
     i.Onkeydown←'##.AutoComplete.OnKeyDown'
     l←p H.New'div'
     l.class←'acList'
     l.Items←ai
     l.Style←BuildCSS 0
     i.AutoCompleteElement←l
     0
 }
