 Init←{
     ⍝ ⍺ ←→ Input element
     ⍝ ⍵ ←→ Autocomplete Items
     ⎕THIS.H←##.Main
     ⍺.autocomplete←'off'
     ⍺.Onkeydown←'##.AutoComplete.OnKeyDown'
     l←⍺.Parent H.New'div'
     l.class←'acList'
     l.Items←⍵
     l.Style←BuildCSS 0
     ⍺.AutoCompleteElement←l
     0
 }
